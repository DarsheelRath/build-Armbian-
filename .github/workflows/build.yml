name: Build Armbian Image
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: armbian/build@main
        with:
          armbian_token: ${{ secrets.GITHUB_TOKEN }}
          armbian_board: "orangepizero2w"
          armbian_release: "trixie"
          armbian_kernel_branch: "current"      # Correct parameter for the experimental kernel
          armbian_target: "build"
          armbian_ui: "minimal"
          armbian_runner_clean: "yes"
        env:
          BOOTFS_TYPE: "fat"                    # Forces the framework to format partition 1 as FAT32
